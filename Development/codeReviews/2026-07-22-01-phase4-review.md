# 2026-07-22 — Phase 4 代码审查报告

## 审查方式

三路并行子代理审查：设计一致性、安全架构、代码质量。

## 审查结果汇总

| 严重度 | 数量 | 已修复 | 说明 |
|--------|------|--------|------|
| CRITICAL | 4 | 4 | 类型不匹配、SSE解析缺陷、Markdown渲染bug |
| HIGH | 8 | 8 | 安全注入、401循环、资源泄漏 |
| MEDIUM | 22 | 8 | 错误处理、死代码、超时缺失 |
| LOW | 18 | 4 | 样式细节、文档建议 |

## 关键修复项

### CRITICAL

| 文件 | 问题 | 修复 |
|------|------|------|
| `types/index.ts:187` | `supportingEvidenceIds` 类型 `string[]` 与后端 `String` 不匹配 | 改为 `string` |
| `api/sse.ts:56` | SSE 多行 `data:` 字段仅保留最后一行 | 追加累积 `eventData += '\n' + ...` |
| `api/sse.ts:72` | SSE 流结束时 `buffer` 中残留数据丢弃 | while 后 flush `eventType`/`eventData` |
| `MarkdownRenderer.tsx:36-39` | 有序列表 `li` 未包裹 `<ol>` | 用标记符区分 ul/ol，分别包裹 |

### HIGH

| 文件 | 问题 | 修复 |
|------|------|------|
| `MarkdownRenderer.tsx:32` | `javascript:` URL 可注入 | Scheme 白名单 `https?:/\/#/mailto:` + `rel="noopener noreferrer"` |
| `api/client.ts:27-29` | 401 重定向覆盖登录页错误提示 | 检查 `window.location.pathname !== '/login'` |
| `api/sse.ts:26` | SSE 连接 401 无重定向 | 添加 401 检查 + 重定向 |
| `DiagnosisWorkspace.tsx:57` | 组件卸载时 AbortController 未清理 | 添加 `useEffect` 清理函数 |

### MEDIUM

| 文件 | 问题 | 修复 |
|------|------|------|
| `api/client.ts:23` | GET 请求误带 `Content-Type: application/json` | 仅 `options.body` 存在时设置 |
| `LoginPage.tsx:4,9` | 死 import `getSystemStatus`、预填用户名 `admin` | 移除 import、清空默认值 |
| `SettingsPage.tsx:94,304,405` | 三处 `handleDelete` 无 try-catch | 添加错误处理 |
| `SettingsPage.tsx:98` | `alert()` 用于连接测试反馈 | 改为内联状态消息 |

## 未修复项（v1 之后改进）

| 问题 | 原因 |
|------|------|
| 无路由守卫（AuthGuard） | 需要 `/auth/me` 端点支持 |
| SetupWizard 未检查 bootstrapped 状态 | 与路由守卫同源 |
| 无 CSP header | 需 Nginx/Spring Boot 层面配置 |
| 无 fetch 超时 | 非阻塞，后续加 `AbortSignal.timeout()` |
| SSE 无重连逻辑 | 后端已有 replay 端点，v2 实现 |
| MarkdownRenderer 自定义解析器脆弱性 | 当前覆盖诊断报告格式足够，复杂场景换 `marked` + `DOMPurify` |
| 有序列表 `<ol>` 包裹依赖标记符 hack | 正则区分 ul/ol 可行性有限，复杂场景换正式库 |
