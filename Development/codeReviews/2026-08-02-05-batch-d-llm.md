# 2026-08-02 批次 D llm 模块修复审查

**范围**：feat/phase8-llm 分支（11 文件：infra 异常移动 5、OpenAiCompatibleChatModel、ModelRouter、pom、测试 2）
**审查方式**：llm 专长子代理审查 git diff
**审查结论**：无 P0/P1，通过可合入；1 P2 留后 + 2 P3（1 已修）

---

## 修复内容

### P1（基线审查遗留）

- **LLM 调用链路零测试** — OpenAiCompatibleChatModel 6 个 MockWebServer 全链路测试（成功解析、429 映射、401、500、空 choices、/v1 归一化）；ModelRouter +2（CUSTOM 无 baseUrl 抛异常、带 baseUrl 正常）
- **onStatus 错误映射** — 429 → LlmRateLimitedException（提取 Retry-After）；其他 4xx → IllegalArgumentException；5xx → IllegalStateException。`LlmRateLimitedException` 此前定义了从未使用
- **CUSTOM 空 baseUrl** — 不再返回空串（相对路径请求），抛 IllegalArgumentException 硬失败

### 架构（正确性修复）

- 异常体系从 infra internal 移到 api 层 — 跨模块契约属于 api；internal 3 个异常补 import，无遗漏（全局搜索确认）

## 审查确认（无缺陷）

- onStatus 异常直接传播不包装；Retry-After 提取正确（HTTP-date 格式也能兜底）
- /v1 归一化顺序正确（无 /v1/v1 双重路径）
- MockWebServer 测试真实收发无假阳性

## 审查遗留（不阻塞）

1. **P2**：CUSTOM 空 baseUrl 校验在调用时而非保存时 — 保存成功但测试/诊断时抛错。LlmController.update 建议后续加保存时校验（当前 /test 有 try/catch 兜底）
2. **P3**：180s read timeout 硬编码 — 后续迭代加配置

## 验证

- 全量测试 ✓（BUILD SUCCESS，含 8 个新测试）
- 注意：`mvn install` 会触发 OWASP dependency-check（NVD 下载可卡数分钟）— 构建用 `-Ddependency-check.skip=true`
