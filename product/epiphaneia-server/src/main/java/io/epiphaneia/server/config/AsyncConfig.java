package io.epiphaneia.server.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class AsyncConfig {
    // @EnableAsync on EpiphaneiaApplication; virtual threads used by default on Boot 4.x
}
