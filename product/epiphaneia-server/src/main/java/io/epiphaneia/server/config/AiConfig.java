package io.epiphaneia.server.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class AiConfig {
    // ChatClient beans defined in Development phase via Spring AI auto-config
}
